version: '2'

linters:
  default: standard
  enable:
    - bodyclose
    - errorlint
    - gosec
    - misspell
    - noctx
    - revive
    - sqlclosecheck
    - rowserrcheck
    - unconvert

formatters:
  enable:
    - gofmt
    - goimports
