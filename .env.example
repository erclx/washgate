# Local development defaults. docker-compose.yml applies the same values when a variable is unset.
DB_NAME=washgate
DB_USER=washgate
DB_PASSWORD=washgate
DB_ROOT_PASSWORD=washgate-root
MARIADB_PORT=3306
